{
  "name": "netlify-discorder",
  "version": "1.0.0",
  "description": "This serverless function sends a Discord message when a defined Github repo is starred.",
  "main": "index.js",
  "scripts": {
    "push": "git add . && git commit -m \"commit\" && git push",
    "deploy": "npm run push && netlify deploy --prod"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "slimbot": "^5.1.0"
  }
}
